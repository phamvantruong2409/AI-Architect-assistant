import { ComingSoon } from "@/components/layout/ComingSoon";
import { ImageIcon } from "@/components/layout/icons";

export default function ReviewPage() {
  return (
    <ComingSoon
      icon={ImageIcon}
      title="Đánh giá Render"
      description="Upload ảnh render để nhận xét ánh sáng, vật liệu và góc camera sẽ có trong Giai đoạn 2 của lộ trình."
    />
  );
}
