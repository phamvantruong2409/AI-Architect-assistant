import { ComingSoon } from "@/components/layout/ComingSoon";
import { WandIcon } from "@/components/layout/icons";

export default function PromptRenderPage() {
  return (
    <ComingSoon
      icon={WandIcon}
      title="Sinh Prompt Render"
      description="Tạo prompt tối ưu cho D5 Render, Lumion, Enscape sẽ có trong Giai đoạn 2 của lộ trình."
    />
  );
}
